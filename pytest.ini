[pytest]
pythonpath = .
# pytest.ini  (place at project root)
[pytest]
testpaths = tests
pythonpath = .

# .pylintrc  (place at project root)
[MESSAGES CONTROL]
disable =
    missing-module-docstring,
    missing-class-docstring,
    missing-function-docstring
